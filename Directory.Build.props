<Project>
  <!--
    Local debugging support:
    - Replaces NuGet packages with local CodeCasa projects
    - Requires sibling checkout of the CodeCasa repository
    - Disabled by default
  -->
  <PropertyGroup>
    <UseLocalLibs>false</UseLocalLibs>
  </PropertyGroup>
</Project>